<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title> Anmelden - Schwarzes Bügelbrett </title>

  @vite(['resources/css/app.css', 'resources/js/app.js'])
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  <style>
    .container-lg {
      background-color: #f8f9fa;
      border-width: 2px !important; /*https://stackoverflow.com/questions/48506610/bootstrap-4-border-utilities*/
    }
  </style>
</head>
<body class="bg-body-secondary d-flex flex-column">
  @include('partials/header')

  <main class="mt-4">
    <div class="d-flex justify-content-center align-items-center">
      <div class="container-lg w-50 shadow rounded mb-4 mx-5 p-5 bg-light">
        <h2 class="mb-4 d-flex justify-content-center">Anmelden</h2>
        <div class="d-flex justify-content-center">
          @if(success != undefinded)
            <div class="alert alert-success d-inline" role="alert">
              {{ success }}
            </div>
          @end
          @if(error != undefined)
            <div class="alert alert-danger d-inline" role="alert">
              {{ error }}
            </div>
          @end
        </div>
        <form action="/anmelden" method="post">
          {{csrfField()}}
          <div class="row mb-2 d-flex justify-content-center">
            <div class="col-md-6">
              <label for="email" class="form-label">E-mail:</label>
              <input type="email" class="form-control shadow" id="email" name="email" placeholder="E-Mail">
              @inputError('email')
                <p class="text-danger mt-2">Bitte geben Sie eine gültige E-Mail-Adresse ein.</p>
              @end
            </div>
          </div>
          <div class="row mb-4 d-flex justify-content-center">
            <div class="col-md-6">
              <label for="password" class="form-label">Passwort:</label>
              <input type="password" class="form-control shadow" id="password" name="password" placeholder="Passwort">
              @inputError('password')
                <p class="text-danger mt-2">Das Passwortfeld muss definiert sein.</p>
              @end
            </div>
          </div>
          <div class="col-md-12 mb-4 d-flex d-flex justify-content-center">
            <button type="submit" class="btn btn-success shadow">Einloggen</button>
          </div>
        </form>
        <div class="row justify-content-between">
          <div class="col-auto mb-2">
            <div class="d-flex align-items-center">
              <p class="mb-0 me-2">Neu?</p>
              <a href="/registrieren" class="link-primary link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover">Hier geht’s zur Registrierung</a>
            </div>
            <div class="col-auto mt-2">
              <a href="/passwort_vergessen" class="link-danger link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover">Passwort vergessen?</a>
            </div>
          </div>
          <div class="col-auto">
            <a href="/home" class="link-danger link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover">Als Gast fortfahren</a>
          </div>
        </div>
      </div>
    </div>
  </main>

  @include('partials/footer')
</body>
</html>
