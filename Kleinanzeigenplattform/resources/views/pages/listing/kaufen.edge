<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title> AdonisJS - A fully featured web framework for Node.js </title>

  @vite(['resources/css/app.css', 'resources/js/app.js'])
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</head>
<body class="bg-body-secondary d-flex flex-column">
  @include('partials/header')

  <main class="mt-4">
    <div class="d-flex justify-content-center align-items-center">
      <div class="container-lg w-50 shadow rounded mb-4 mx-5 p-5 bg-light">
        <h3 class="mb-3">{{ seller.username }} | {{ listing.title }}</h3>
        <form action="/home/artikel/{{ listing.listing_id }}/kaufen" method="post">
          {{csrfField()}}
          <div class="row mb-3">
            <div class="col-md-6">
              <label for="kartennummer" class="form-label">Kartennummer:</label>
              <input type="text" class="form-control shadow" id="kartennummer" name="kartennummer" placeholder="Kartennummer">
            </div>
            <div class="col-md-6">
              <label for="verfallsdatum" class="form-label">Verfallsdatum:</label>
              <input type="text" class="form-control shadow" id="verfallsdatum" name="verfallsdatum" placeholder="Verfallsdatum">
            </div>
          </div>
          <div class="row mb-4">
            <div class="col-md-6">
              <label for="cvcode" class="form-label">CV Code:</label>
              <input type="text" class="form-control shadow" id="cvcode" name="cvcode" placeholder="CV Code">
            </div>
            <div class="col-md-6 d-flex justify-content-end align-items-end">
              <button type="submit" class="btn btn-success shadow">Kaufen</button>
            </div>
          </div>
          @if(success != undefinded)
            <div class="alert alert-success" role="alert">
              {{ success }}
            </div>
          @end
          @if(error != undefined)
            <div class="alert alert-danger" role="alert">
              {{ error }}
            </div>
          @end
        </form>
      </div>
    </div>
  </main>

  @include('partials/footer')
</body>
</html>
