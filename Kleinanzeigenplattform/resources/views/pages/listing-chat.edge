<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title> AdonisJS - A fully featured web framework for Node.js </title>

  @vite(['resources/css/app.css', 'resources/js/app.js'])
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  <script src="{{ asset("resources/js/app.js") }}"></script>
</head>
<body class="bg-body-secondary">
  @include('partials/header')

  <div class="d-flex justify-content-center align-items-center">
    <div id="chatBox" class="container-lg w-50 shadow rounded my-4 mx-5 p-3 bg-light chatBoxSize overflow-auto">
      <div class="d-flex flex-row sticky-top bg-light border rounded shadow p-2 mb-4">
        @if (user.user_id != listing.user_id)
           <h3>{{ receiverUsername[0].receiver_username }} | {{ listing.title }}</h3>
        @else
          <h3>{{ allMessages[0].sender_username }} | {{ listing.title }}</h3>
        @end

      </div>
      @each (message in allMessages)
        <div class="d-flex {{ message.sender_id == user.user_id ? 'justify-content-end' : 'justify-content-start' }}">
          <div class="message-bubble text-break shadow {{ message.sender_id == user.user_id ? 'sender' : 'receiver' }}">
              <p class="mb-0">{{ message.message_text }}</p>
              <p class="d-flex justify-content-end fw-lighter small-font my-0">{{ message.timestamp }}</p>
          </div>
        </div>
      @end
    </div>
  </div>

  <div class="d-flex justify-content-center py-3 border-bottom bg-dark-subtle">
    <a href="/home/konto/nachrichten" class="btn btn-danger shadow me-2">Zurück</a>
    <form action="/home/artikel/{{ listing.listing_id }}/user/{{ allMessages[0] ? allMessages[0].sender_id : user.user_id }}/chat" method="POST" class="d-flex">
      {{ csrfField() }}
      <input class="form-control shadow me-2" type="search" placeholder="Schreibe eine Nachricht..." name="nachricht" aria-label="Schreibe eine Nachricht..." maxlength="100" autofocus>
      <button class="btn btn-success shadow me-2" type="submit">Senden</button>
    </form>
      @if (user.user_id != listing.user_id)
        <button class="btn btn-warning shadow" onclick="sendWasLetztePreis()">Was letzte Preis?</button>
      @end
  </div>

  @include('partials/footer')
</body>
</html>
