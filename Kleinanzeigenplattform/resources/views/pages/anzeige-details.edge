<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title> AdonisJS - A fully featured web framework for Node.js </title>

  @vite(['resources/css/app.css', 'resources/js/app.js'])
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</head>
<body class="bg-body-secondary">
  @include('partials/header')

  <div class="d-flex justify-content-center align-items-center">
    <div class="container-lg w-75 shadow rounded my-4 mx-5 p-5 bg-light">
      <div class="row">
        <div class="col-md-7">
          <div id="ImageCarouselIndicators" class="carousel carousel-dark slide">
            <div class="carousel-indicators">
              @each((image, index) in images)
              <button type="button" data-bs-target="#ImageCarouselIndicators" data-bs-slide-to="{{ index }}" class="{{ index === 0 ? 'active' : '' }}" aria-current="{{ index === 0 }}" aria-label="Slide {{ index }}"></button>
              @end
            </div>
            <div class="carousel-inner">
              @each((image, index) in images)
              <div class="carousel-item {{ index === 0 ? 'active' : '' }}">
                <img src="/uploads/{{ image.link }}" class="d-block object-fit-cover w-100 h-100" alt="{{ image.link }}">
              </div>
              @end
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#ImageCarouselIndicators" data-bs-slide="prev">
              <span class="carousel-control-prev-icon" aria-hidden="true"></span>
              <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#ImageCarouselIndicators" data-bs-slide="next">
              <span class="carousel-control-next-icon" aria-hidden="true"></span>
              <span class="visually-hidden">Next</span>
            </button>
          </div>
        </div>
        <div class="col-md-5 d-flex flex-column justify-content-between bg-body-secondary rounded shadow p-4">
          <div class="d-flex flex-row justify-content-between">
            <h3 class="text-break fw-bold mb-4 me-2">{{ listing.title }}</h3>
            <h3 class="fw-bold d-flex justify-content-end">{{ listing.price }}€</h3>
          </div>
          <div class="d-flex justify-content-between">
            <div class="d-flex justify-content-start align-items-start">
              <img src="/uploads/{{ seller.profile_image }}" class="d-block object-fit-cover profilePicture border rounded-circle border-2 border-dark me-2" alt="Profilbild">
              <p class="fw-bold mb-3 mt-auto">{{ seller.username }}</p>
            </div>
            @if (user && user.user_id != seller.user_id)
              <form action="/home/artikel/{{ listing.listing_id }}/user/{{ user.user_id }}/chat" method="GET">
                {{csrfField()}}
                <button class="btn btn-primary" type="submit">Nachricht schreiben</button>
              </form>
            @end
          </div>
          <p class="text-break overflow-auto mt-4">{{ listing.description }}</p>
          <div>
          @if (user && user.user_id != seller.user_id)
            <div class="d-flex justify-content-end">
              <a href="/home/artikel/{{ listing.listing_id }}/kaufen" class="btn btn-success shadow">Artikel kaufen</a>
            </div>
          @end
          @if (user && user.user_id === seller.user_id)
            <div class="d-flex justify-content-end">
            <form action="/home/artikel/{{ listing.listing_id }}" class="me-2" method="post">
              {{csrfField()}}
              @if (listing.active === 1)
                <button class="btn btn-warning" type="submit">Deaktivieren</button>
              @else
                <button class="btn btn-success" type="submit">Aktivieren</button>
              @end
            </form>
            <form action="/home/konto/eigene_anzeigen/{{ listing.listing_id }}" method="POST">
              {{csrfField()}}
              <button class="btn btn-danger" type="submit">Löschen</button>
            </form>
          </div>
          @end
          </div>
        </div>
      </div>
    </div>
  </div>

  @include('partials/footer')
</body>
</html>
