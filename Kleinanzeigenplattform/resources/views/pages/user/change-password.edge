<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title> Passwort ändern - Schwarzes Bügelbrett </title>

  @vite(['resources/css/app.css', 'resources/js/app.js'])
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</head>
<body class="bg-body-secondary d-flex flex-column">
@include('partials/header')
  <main class="mt-4">
    <div class="d-flex justify-content-center align-items-center">
      <div class="container-lg w-75 shadow rounded mb-4 mx-5 p-3 bg-light">
        <h1 class="my-3 ms-2">Passwort zurücksetzen:</h1>
        @if(error != undefined)
          <div class="alert alert-danger d-inline-block ms-2" role="alert">
            {{ error }}
          </div>
        @end
        <form action="/passwort_zuruecksaetzen/{{ user_id }}/{{ verificationToken }}" method="post" class="row d-flex justify-content-center">
          {{csrfField()}}
          <div class="col-md-5 mb-2">
            <label for="password" class="form-label">Passwort:</label>
            <input type="password" class="form-control shadow" id="password" name="password" placeholder="Passwort" minlength="8" maxlength="16">
            @inputError('password')
              <p class="text-danger mt-2">Anforderungen: ein Kleinbuchstabe, ein Großbuchstabe, eine Zahl, ein Symbol, zwischen 8 und 16 Zeichen.</p>
            @end
          </div>
          <div class="col-md-5 mb-4">
            <label for="repeat_password" class="form-label">Passwort wiederholen:</label>
            <input type="password" class="form-control shadow" id="repeat_password" name="repeat_password" placeholder="Passwort wiederholen" minlength="1" maxlength="15">
            @inputError('repeat_password')
              <p class="text-danger mt-2">Anforderungen: ein Kleinbuchstabe, ein Großbuchstabe, eine Zahl, ein Symbol, zwischen 8 und 16 Zeichen.</p>
            @end
          </div>
          <div class="col-md-12 mb-4 d-flex justify-content-center">
            <button type="submit" class="btn btn-success shadow">Zurücksetzen</button>
          </div>
        </form>
      </div>
    </div>
  </main>

  @include('partials/footer')
</body>
</html>
