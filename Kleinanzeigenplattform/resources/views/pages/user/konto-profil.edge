<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title> AdonisJS - A fully featured web framework for Node.js </title>

  @vite(['resources/css/app.css', 'resources/js/app.js'])
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <script src="{{ asset("resources/js/app.js") }}"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</head>
<body class="bg-body-secondary d-flex flex-column">
  @include('partials/header')

  <main class="mt-4">
    <div class="d-flex justify-content-center align-items-center">
      <div class="container-lg w-75 shadow rounded mb-4 mx-5 p-3 bg-light">
        <div class="container mt-3">
          <div class="row">
            <div class="d-flex justify-content-center align-items-start">
              <h2 class="mb-3 mt-auto">Profil: {{ user.username }}</h2>
              <img src="/uploads/{{ user.profile_image }}" class="d-block object-fit-cover profilePicture border rounded-circle border-2 border-dark mx-2 my-2" alt="{{ user.profile_image }}">
            </div>
          </div>
          <div class="row">
            <div class="col-md-12">
              <form action="/home/konto/profil" method="POST" enctype="multipart/form-data">
                {{csrfField()}}
                <div class="row">
                  <div class="col-md-6 mb-3">
                    <label for="profile_picture" class="form-label">Profilbild:</label>
                    <input type="file" class="form-control shadow" name="profile_picture" id="profile_picture" onchange="previewImages()" disabled>
                    <div id="image-preview" class="d-flex flex-row"></div>
                  </div>
                  <div class="col-md-6 mb-3">
                    <label for="email" class="form-label">E-Mail:</label>
                    <input type="text" id="email" class="form-control shadow" name="email" placeholder="{{ user.email }}" value="{{ user.email }}" disabled>
                  </div>
                </div>
                <div class="row">
                  <div class="col-md-6 mb-3">
                    <label for="firstname" class="form-label">Vorname:</label>
                    <input type="text" id="firstname" class="form-control shadow" name="firstname" placeholder="{{ user.firstname }}" value="{{ user.firstname }}" disabled>
                  </div>
                  <div class="col-md-6">
                    <label for="lastname" class="form-label">Nachname:</label>
                    <input type="text" id="lastname" class="form-control shadow" name="lastname" placeholder="{{ user.lastname }}" value="{{ user.lastname }}" disabled>
                  </div>
                </div>
                @if(error != undefined)
                  <p class="d-flex justify-content-center text-danger">{{error}}</p>
                @end
                <div class="col-md-12 d-flex justify-content-center align-items-cente mt-4 mb-4">
                  <button type="button" id="changeDataBtn" class="btn btn-primary shadow" onclick="enableInputs()">Daten ändern</button>
                  <button type="button" id="cancelBtn" class="btn btn-danger shadow d-none me-3" onclick="disableInputs()">Abbrechen</button>
                  <button type="submit" id="doChangeBtn" class="btn btn-success shadow d-none" onclick="enableInputs()">Ändern</button>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>

  @include('partials/footer')
</body>
</html>
